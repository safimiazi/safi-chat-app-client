import { Dialog } from '@mui/material';
import React from 'react';

const CreateGroup = () => {
    return (
        <Dialog fullWidth>
            
        </Dialog>
    );
};

export default CreateGroup;